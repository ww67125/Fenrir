全局锁
数据库实例加锁，运行Flush tables with read lock 命令加全局锁，会让整库处于只读状态。
此时数据的增删改，ddl语句和更新类事务的提交语句会被阻塞。

应用场景
全库逻辑备份，将整个表select导出sql语句。操作危险不推荐使用，只有myisam使用。
推荐使用mysqldump -single-transaction参数备份整库。（前提数据库中都是使用事务引擎的表）

表级锁
